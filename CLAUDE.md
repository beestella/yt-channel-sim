# yt-channel-sim

유튜브 채널 운영 시뮬레이터. 패스트뷰 제작사업본부 팀원들이 롱폼/쇼츠 발행량과 조회수로 월 유튜브 운영매출과 수익화(YPP) 달성 시점을 가늠하는 단일 HTML 도구.

- 라이브: https://beestella.github.io/yt-channel-sim/
- 배포: `main` 브랜치의 `index.html`이 그대로 서빙됨 (GitHub Pages, Branch main / root). 빌드 없음.
- 수정 → 커밋 → `git push origin main` 하면 1~2분 뒤 자동 반영.
- "배포해"라고 하면 항상 이 저장소에 push하는 것을 뜻함. 다른 호스팅은 쓰지 않음.

## 파일
- `index.html`: 전체 앱. CSS·JS·Pretendard 폰트(서브셋, base64 @font-face)까지 한 파일. 외부 리소스 없음.
- 폰트는 페이지에 쓰인 글자만 서브셋했으므로 새 한글 글자를 추가하면 그 글자만 시스템 폰트로 뜬다. 문구를 크게 바꾸면 서브셋을 다시 만들 것: 원본 PretendardVariable.woff2를 받아 `pyftsubset` (fonttools + brotli)으로 페이지 텍스트 전체 글자 + 숫자·기호를 포함해 woff2 생성 → base64 → `<style>` 안 @font-face의 data URI 교체.

## 화면 구성 (위에서 아래)
1. 헤더 카드: 제목 "유튜브 채널 운영 시뮬레이터", 유튜브 운영매출 정의, 수익 항목 칩 3개(롱폼 보기 페이지 광고 수익 / 쇼츠 피드 광고 수익 / YouTube Premium 시청 배분 수익)
2. "먼저, 채널이 수익화 상태인가요?" 라디오 카드 `#yppstate` (수익화 전 / 완료). 캡션: 구독자 1,000명 조건은 이미 채운 것으로 계산
3. "이럴 때 사용하면 좋아요" 안내 칩 3개 (모드별, 수익화 최단 경로 칩은 수익화 전에서만 표시)
4. 시뮬레이터 카드: 모드 탭 3개(발행량으로 계산 `fwd` / 목표에서 역산 `rev` / 수익화 최단 경로 `ypp`) → 기간 `#period` 1/3/6/12개월(기본 1) → 결과 타일 `#tiles` → 입력(롱폼/쇼츠 편수·편당 조회수 슬라이더+숫자, RPM은 잠금 상태이고 "수정" 버튼으로만 편집) → 월 목표 매출 `goal` → 모드별 결과 영역 `#m-fwd`(월별 표, 레버 효과) / `#m-rev`(역산 3안 타일) / `#m-ypp`(수익화 그리드)
5. 자사 쇼츠 데이터 분석 표(채널 A~D), 참고 100만 회당 매출, 근거 링크, 푸터 저작권

## 기준값과 근거 (근거 없는 수치 추가 금지)
- 롱폼 RPM 1,250원: 2026년 국내 자료 두 건의 겹침 구간 중앙값 (유튜브메모 2026-02 예능·게임 500~1,500원 / 스타트벅스 2026-07 롱폼 1,000~5,000원)
- 쇼츠 RPM 149원: 자사 쇼츠 2025-07 애드센스 데이터 가중평균 (채널 A 130 / B 176 / C 193 / D 235원)
- 기본 입력: 롱폼 8편×10만 회, 쇼츠 20편×15만 회, 월 목표 300만원, 수익화 전, 1개월
- 수익화 조건: 구독자 1,000명 + 최근 90일 쇼츠 1,000만 회 (support.google.com/youtube/answer/72851). 로직: 최근 3개월 누적 쇼츠 조회수가 1,000만을 넘는 첫 달 = 달성, 매출은 그 다음 달부터 계산, 승인 전 매출은 0
- 새 자료를 쓸 때는 2026년 자료만, 출처와 일자를 근거 섹션에 함께 기재

## 규칙
- 자사 채널 실명(스광·오토포스트·다이슈·피플박스) 금지. 채널 A/B/C/D로만 표기.
- "실측" 대신 "데이터 분석". "광고 매출"·"광고 수익이 들어오지 않아요" 같이 외부 광고로 오해될 문구 금지. 조회수 기반 자동정산 수익은 "유튜브 운영매출"로 통일.
- 엠대시(—) 사용 금지. UI 카피는 해요체.
- 보수/기준/낙관 같은 시나리오 분기, 성장률 입력 등 복잡도를 높이는 입력은 넣지 않음. 팀원이 쉽게 쓰는 것이 우선.
- 숫자는 천 단위 콤마. 비중이 큰 포맷(롱폼/쇼츠)이 파란색.
- 디자인은 토스 디자인 시스템 기준(blue-500 #3182F6, grey-900 #191F28, 24px 카드, Pretendard).
- 맨 하단 저작권 "© 2026 패스트뷰 제작사업본부 · Jiwon Kim" 유지.

## 수정 후 점검
- 금칙어 검색: `grep -nE "스광|오토포스트|다이슈|피플박스|실측|—" index.html` 결과가 0건이어야 함
- id 중복 없음, 입력 0·빈 값·문자 입력 시 NaN 미표시, 모바일(375px) 가로 스크롤 없음
- 가능하면 브라우저(Playwright 등)로 세 모드와 기간 4개를 한 번씩 눌러 확인
